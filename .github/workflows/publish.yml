name: Publish

# Dispatch-only. Every run builds the Windows and Linux Writer with NativeAOT, stamps both
# with the next preview version, and attaches each platform's single executable to the run
# as its own zip artifact, then drafts a GitHub pre-release carrying both:
#
#   broiler-writer-win-x64-<version>.zip    Broiler.Writer.Windows.exe
#   broiler-writer-linux-x64-<version>.zip  Broiler.Writer.Linux
#
# NativeAOT is what makes the executable the whole application: self-contained, trimmed,
# no .NET runtime to install, nothing beside it. Nothing is pushed to a feed. The draft
# release (eng/release-draft.sh) zips each executable as Broiler.Writer-<version>-<rid>.zip
# and stays private until someone publishes it.
#
# nuget-source picks the feed the Broiler.* dependencies are restored from. NuGet.config
# resolves everything from nuget.org; broiler-github maps Broiler.* to the Broiler-Platform
# GitHub Packages feed instead, for components published there but not yet to nuget.org.
#
# The version is BroilerWriterVersion (Directory.Build.props) raised past every earlier
# run's writer-v* tag, and a successful run tags its commit, so preview numbers only ever
# increase and never name two builds. See eng/resolve-preview-version.mjs.
on:
  workflow_dispatch:
    inputs:
      nuget-source:
        description: Feed to restore the Broiler.* dependency packages from
        type: choice
        options: [nuget.org, broiler-github]
        default: nuget.org
      version-suffix:
        description: >-
          Optional preview.N override (unused, and at least the next preview). Empty picks
          the next preview automatically.
        type: string
        default: ''

permissions:
  contents: read
  packages: read

# One run at a time, so two runs cannot pick the same preview number.
concurrency:
  group: publish
  cancel-in-progress: false

env:
  DOTNET_NOLOGO: 'true'
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 'true'

jobs:
  version:
    name: Resolve preview version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - name: Check out
        uses: actions/checkout@v7

      - name: Set up
        uses: ./.github/actions/setup-broiler

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'

      - name: Test the version resolver
        run: node --test eng/resolve-preview-version.test.mjs

      - name: Resolve version
        id: version
        env:
          VERSION_SUFFIX: ${{ inputs.version-suffix }}
        run: node eng/resolve-preview-version.mjs

  build:
    name: ${{ matrix.rid }} (NativeAOT)
    needs: version
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    defaults:
      run:
        shell: bash

    strategy:
      fail-fast: true
      matrix:
        include:
          # The pinned configurations are the ones FolderProfile.pubxml names and they
          # carry the runtime identifier NativeAOT needs.
          - os: windows-latest
            rid: win-x64
            project: src/Broiler.Writer.Windows/Broiler.Writer.Windows.csproj
            configuration: Release-Windows
            executable: Broiler.Writer.Windows.exe
          # NativeAOT cannot cross-compile, so the Linux binary links on Linux.
          - os: ubuntu-latest
            rid: linux-x64
            project: src/Broiler.Writer.Linux/Broiler.Writer.Linux.csproj
            configuration: Release-Linux
            executable: Broiler.Writer.Linux

    env:
      VERSION: ${{ needs.version.outputs.version }}
      OUT: artifacts/publish/${{ matrix.rid }}
      # Read by NuGet for the github-broiler source in NuGet.config. Unused, and harmless,
      # when Broiler.* restores from nuget.org.
      NuGetPackageSourceCredentials_github-broiler: Username=${{ github.actor }};Password=${{ secrets.GITHUB_TOKEN }};ValidAuthenticationTypes=Basic

    steps:
      - name: Check out
        uses: actions/checkout@v7

      - name: Set up
        uses: ./.github/actions/setup-broiler

      # NuGet.config declares github-broiler but maps every package to nuget.org. A more
      # specific Broiler.* pattern wins, so adding one moves exactly the Broiler packages.
      - name: Restore Broiler.* from ${{ inputs.nuget-source }}
        if: inputs.nuget-source == 'broiler-github'
        run: |
          sed -i '/<\/packageSourceMapping>/i\    <packageSource key="github-broiler"><package pattern="Broiler.*" /></packageSource>' NuGet.config
          grep -q 'key="github-broiler"><package pattern="Broiler' NuGet.config

      # ILCompiler shells out to a platform linker. windows-latest carries the MSVC C++
      # tools; on Linux the toolchain is clang plus zlib headers.
      - name: Install the NativeAOT prerequisites
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y clang zlib1g-dev

      # PublishAot implies self-contained and trimmed: one native binary, no .NET runtime
      # to install. BroilerWriterVersion flows into Version, so the About dialog and the
      # file version match the artifact.
      - name: Publish
        run: |
          dotnet publish '${{ matrix.project }}' \
            -c '${{ matrix.configuration }}' \
            -p:PublishAot=true \
            -p:BroilerWriterVersion="$VERSION" \
            -o "$OUT"

      # The artifact is the executable alone. Symbols (.pdb, and Linux's detached .dbg) and
      # XML docs are dead weight next to it; anything else would be a file the application
      # needs, so shipping without it would be broken - fail rather than drop it.
      - name: Check that the executable is the whole application
        run: |
          others=$(find "$OUT" -type f ! -name '${{ matrix.executable }}' \
            ! -name '*.pdb' ! -name '*.dbg' ! -name '*.xml')
          if [ ! -f "$OUT/${{ matrix.executable }}" ] || [ -n "$others" ]; then
            echo "::error::Expected only ${{ matrix.executable }} in the publish output."
            find "$OUT" -type f
            exit 1
          fi

      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          name: broiler-writer-${{ matrix.rid }}-${{ env.VERSION }}
          path: ${{ env.OUT }}/${{ matrix.executable }}
          if-no-files-found: error

  release:
    name: Tag and draft release ${{ needs.version.outputs.tag }}
    needs: [version, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      VERSION: ${{ needs.version.outputs.version }}
      TAG: ${{ needs.version.outputs.tag }}

    steps:
      - name: Check out
        uses: actions/checkout@v7

      # Only after both platforms built: a failed run leaves its number free. The tag is
      # pushed here rather than left to the release, because a draft release does not
      # create its tag until it is published - and the next run counts tags.
      - name: Tag the commit
        run: |
          git tag "$TAG" "$GITHUB_SHA"
          git push origin "refs/tags/$TAG"

      - name: Download the executables
        uses: actions/download-artifact@v8
        with:
          pattern: broiler-writer-*
          path: artifacts

      # A draft pre-release: nothing is public until someone reviews and publishes it.
      - name: Draft the release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash eng/release-draft.sh "$VERSION" artifacts

      - name: Summary
        run: |
          {
            echo "### Broiler.Writer $VERSION"
            echo
            echo "Dependencies restored from \`${{ inputs.nuget-source }}\`; commit tagged \`$TAG\`."
            echo
            echo "A draft pre-release **Broiler Writer $VERSION** was created with"
            echo "\`Broiler.Writer-$VERSION-win-x64.zip\` and \`Broiler.Writer-$VERSION-linux-x64.zip\`."
            echo "Review it under Releases and publish it there."
          } >> "$GITHUB_STEP_SUMMARY"
